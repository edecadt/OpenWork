import React from "react";
import { Input } from "./ui/input";

const Header = () => {
    return (
      <header className="bg-gray-200 text-Black p-10"> {}
        <Input/>
      </header>
    );
  };

export default Header;